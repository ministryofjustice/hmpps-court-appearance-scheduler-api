info:
  name: Get integration appearance
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/integrations/court-appearances/{{integration_appearance_id}}"
  auth:
    type: bearer
    token: "{{bearer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
