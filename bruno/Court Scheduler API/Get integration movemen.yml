info:
  name: Get integration movemen
  type: http
  seq: 13

http:
  method: GET
  url: "{{base_url}}/integrations/court-appearance-movements/{{integration_movement_id}}"
  auth:
    type: bearer
    token: "{{bearer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
