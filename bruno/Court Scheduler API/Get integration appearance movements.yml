info:
  name: Get integration appearance movements
  type: http
  seq: 12

http:
  method: GET
  url: "{{base_url}}/integrations/court-appearances/{{integration_appearance_id}}/movements"
  auth:
    type: bearer
    token: "{{bearer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
