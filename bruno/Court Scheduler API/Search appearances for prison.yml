info:
  name: Search appearances for prison
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/search/prisons/{{prison_code}}/court-appearances"
  body:
    type: json
    data: |-
      {
        "start": "{{date_today}}",
        "end": "{{date_next_month}}",
        "status": [
          "SCHEDULED"
        ],
        "external": true,
        "query": null,
        "page": 1,
        "size": 10,
        "sort": "start"
      }
  auth:
    type: bearer
    token: "{{bearer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
