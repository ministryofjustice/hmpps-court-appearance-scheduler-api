info:
  name: Get court appearance history
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/court-appearances/{{court_appearance_id}}/history"
  auth:
    type: bearer
    token: "{{bearer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
