info:
  name: Get court appearance
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/court-appearances/{{court_appearance_id}}"
  auth:
    type: bearer
    token: "{{bearer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
